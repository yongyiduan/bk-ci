parameters:
  - name: receivers
    type: array
    default: [ "${| ci.actor }}" ]

gates:
  - name: 单元测试覆盖率
    rule:
      - ngtestQuality.total_cover_rate >= 100
    notify-on-fail:
      - type: wework-message
        receivers: ${| parameters.receivers }}